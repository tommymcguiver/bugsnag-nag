package main

import (
 "github.com/robfig/cron"
 "fmt"
)

func main(){

	c := cron.New()
	c.AddFunc("0 0 9-17/2 * * *", func() { fmt.Println("Every hour on the half hour") })
	c.Start()
	for _, e := range c.Entries(){
		fmt.Println(e)
	}
}
